export interface ReferenceSave {
  id: string
  userId: string
  referenceId: string
  createdAt: string
}

export interface SpotSave {
  id: string
  userId: string
  spotId: string
  createdAt: string
}
