-- ============================================================
-- RE:FRAME — AI 촬영 가이드 캐시(shooting_guide)
-- 사용법: Supabase 대시보드 > SQL Editor 에 붙여넣고 Run. (여러 번 실행 안전)
--
-- Gemini가 생성한 "이 사진 따라 찍기" 가이드를 레퍼런스 기준으로 한 번만 만들어
-- 캐싱한다 — 재조회 시 Gemini를 다시 호출하지 않고 저장된 값을 그대로 보여준다.
-- 레퍼런스 자체 데이터(이미지+메타데이터)만으로 만들어지는 값이라 조회자와 무관하게
-- 공유되는 캐시로 둔다.
--
-- 참고: 이 컬럼이 없어도 프론트는 정상 동작한다(매번 다시 생성될 뿐 에러 없음).
-- 이 SQL을 실행하면 프론트 코드 변경 없이 바로 캐싱이 시작된다.
-- ============================================================

alter table public."references"
  add column if not exists shooting_guide jsonb;
