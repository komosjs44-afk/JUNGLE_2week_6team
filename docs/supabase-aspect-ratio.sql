-- ============================================================
-- RE:FRAME — 레퍼런스 사진 비율(aspect_ratio)
-- 사용법: Supabase 대시보드 > SQL Editor 에 붙여넣고 Run. (여러 번 실행 안전)
--
-- 업로드 시 사용자가 고른 비율(원본 우선, 또는 1:1/4:5/3:4 프리셋)을 저장해서
-- 피드·상세 화면이 정사각형으로 강제 크롭하지 않고 실제 비율대로 보여줄 수 있게 한다.
--   값 없음(null) → 프론트가 3:4로 대체 (레거시 데이터 호환)
-- ============================================================

alter table public."references"
  add column if not exists aspect_ratio real;
