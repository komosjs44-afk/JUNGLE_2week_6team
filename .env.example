# Supabase (not yet connected — placeholders for future ApiRepository)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# FastAPI backend (not yet connected)
VITE_API_BASE_URL=

# Kakao Maps JavaScript key (get one at https://developers.kakao.com — register this
# app's domain under JavaScript SDK settings, or the map will fail to load)
VITE_KAKAO_MAP_KEY=

# NOTE: Never put Gemini API keys in the frontend.
# Gemini calls must always go through the FastAPI backend.
